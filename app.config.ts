export default defineAppConfig({
  ui: {
    primary: "brandy",
    gray: "neutral",
    formGroup: {
      help: "mt-0 text-sm",
      error: "mt-0 text-sm",
    },
    card: {
      body: {
        base: "h-full",
      },
      ring: "ring-0",
    }
  },
});
